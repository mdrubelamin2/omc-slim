---
description: Dispatch the oracle agent — a second opinion on an architecture, security or data-integrity decision
argument-hint: [the decision to judge, and its constraints]
---

Dispatch the `omc-slim:oracle` agent with the Agent tool, passing the decision below verbatim as its prompt.

It judges a decision rather than a diff, and it argues an assigned opposing side. Relay the verdict and the findings in your reply, because the user cannot see the agent's output. If the task below is empty, ask which decision to judge instead of dispatching.

Task: $ARGUMENTS
