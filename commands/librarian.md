---
description: Dispatch the librarian agent — what is true today outside this repo, every claim sourced
argument-hint: [the external fact, API or prior art to verify]
---

Dispatch the `omc-slim:librarian` agent with the Agent tool, passing the task below verbatim as its prompt.

It reads the installed source on disk before anything written about it, and every load-bearing claim comes back with a source. Relay the answer, the evidence and the caveats in your reply, because the user cannot see the agent's output. If the task below is empty, ask what to verify instead of dispatching.

Task: $ARGUMENTS
