# Progress log — the v1.0 backlog, executed

Task: complete `docs/TODO-v1.0.md`. Started 2026-08-29. Staged as the release
train the backlog itself defines (R0–R6). This file is the state of that run;
re-read it before continuing anything.

Reference by path, never paste contents. Update after every ruling, gate and
wagon.

## Standing decisions for this run

- **No paid runs.** The user chose "build everything, fire nothing paid"
  (2026-08-29). R4's arms are built to fire, budget stated, and left unfired.
  This is also what ruling M2 requires — no publishable ablation on a build with
  known contradictions, and those close only at the end of R1.
- **Nothing outward-facing.** R6 drafts the marketplace listing, the
  awesome-claude-code entry and the write-up. It does not send them.
- **Seven review seats.** Every wagon's gate is read as principal engineer,
  system engineer, AI/ML engineer, context engineer, prompt engineer, Claude Code
  engineer, harness engineer and tech lead. Findings land in this file.

## Confirmed findings (2026-08-29, re-derived this session)

1. **All 19 unverified audit findings still reproduce.** v0.9.1 fixed none —
   the only commit after the 2026-08-28 audit touches `docs/` only. Two
   corrections: B7 is narrower than recorded (the adversarial pass is already
   exempt at every size; the gap is lane-level), C11's span is `:226-231`.
   **B5 is worse than recorded** — `unverified` appears exactly once in
   `skills/deepwork/SKILL.md` and nothing carries the mark into the final
   message, so the waiver is not merely uncapped, it is unreported.
2. **`skills/review/SKILL.md` is 298 tokens OVER the re-injection cap**, not 44
   under it. Real cl100k measurement: 5,298 tokens against 5,000. The gate in
   `scripts/check-coverage.sh` divides chars/4 by a hard-coded 1.135 taken as a
   whole-estate average in the 2026-08-25 audit; that divisor does not hold for
   this file. COMPRESSION-2026-08-28 §3's "margin fragility, not a live defect"
   is therefore wrong, and its ~850-char offload is about a third of what the
   file needs.
3. **The chars/4 correction divisor is itself stale.** Measured today on the
   static set: chars/4 runs +9.4% high against cl100k, not +13.5%. The published
   "~4,075 corrected" understates; real is ~4,197.
4. **Seven live behaviours are pinned by nothing** and would regress silently:
   `explorer`'s `maxTurns: 100`; the `rg -l`/`rg -c`-before-`rg -n` funnel detail
   and the whole `ast-grep` paragraph (COVERAGE.tsv pins only the four-word
   heading); `LIMITATIONS.md`'s compaction-eviction paragraph; `README.md`'s two
   settings; the §9.5 keep/cut table; `simplify/SKILL.md`'s size.
5. **§9 residue: rows 8–25 are all PRESENT. Row 26 is PARTIAL** —
   `deepwork/SKILL.md` shed only 774 chars to a 4,363-char sibling and is now
   *larger* than before the extraction (361 lines against a 332-line target).

## The native surface moved — verified 2026-08-29 against binary 2.1.251

Prior pass ran on 2.1.247 (2026-08-26). Nine facts moved, and three break
published claims. Full ledger goes into `docs/NATIVE.md` at R3.

**Breaks a claim this repo publishes:**

1. **`FileChanged` is a real hook event.** Payload
   `{file_path, event: change|add|unlink}`, chokidar-backed, non-blocking, and
   `SessionStart`/`CwdChanged`/`FileChanged` hooks may return
   `hookSpecificOutput.watchPaths`. The standing refusal "aider watch mode — no
   filesystem-change hook exists" is **factually dead**, and its own reopening
   trigger ("only if the platform ships the missing capability") has fired.
2. **A plugin CAN learn which output style won.** The StatusLine hook's stdin
   carries `output_style: {name}`, and the stream-json `system:init` frame plus
   the control-protocol `initialize` response carry `output_style` and
   `available_output_styles`. `check-output-style.mjs`'s docstring says the hook
   "does not try to learn which style won" as though it were impossible. It is a
   choice now, and the file must say so.
3. **`context: fork` inherits the parent's full system prompt**, so the output
   style DOES govern fork-shaped skills. VIABILITY part II §2's "the
   orchestration style governs only the main loop" is too strong.

**Changes what R3 has to argue:**

4. **Dynamic Workflows are GA on all paid plans** — `agent()`, `pipeline()`,
   `parallel()`, `phase()`, resumable runs, `/workflows` progress UI, and
   plugins may ship a `workflows/` directory. The docs address this plugin by
   name in spirit: *"If you already have an orchestrator built another way… you
   can point Claude at it and ask for a workflow that does the same thing."*
   The seam is real and quotable: workflows have **no mid-run user input** —
   *"For sign-off between stages, run each stage as its own workflow."* That is
   exactly deepwork's gate.
5. **Explore inherits, capped at Opus** (`CLAUDE_CODE_DISABLE_EXPLORE_INHERIT_CAP`
   escapes it). On a Fable session Explore runs on Opus while omc-slim's
   `explorer` inherits uncapped — a newly material difference, not a thin
   residual.
6. **A fifth built-in style, `Proactive`**, shipped with no changelog entry.
7. **`SubagentStop` now carries `last_assistant_message` and `background_tasks`.**
   The first removes the need to parse the transcript at all.
8. **New agent key `experimental.cacheTtl: 5m|1h`** (2.1.248) — the per-agent
   form of the `subagentPromptCacheTtl` the README already documents.
9. **`CLAUDE_CODE_SUBAGENT_MODEL` semantics reversed** (2.1.251): it now sets a
   default rather than overriding, so an agent's own `model:` wins. This bears
   directly on the no-per-agent-model-pinning refusal.

Also recorded, not yet acted on: `observer:` / `observerMessage:` /
`observeSubagents:` exist in shipping code and in no documentation — a
first-party supervisory-critic pattern, unstable, do not build on it.
`disableBundledSkills` is the clean A/B lever for R4's component arms.
`--restricted`, `PreModelSwitch`, `PostModelSwitch` shipped 2.1.248–251.
The skill *listing* budget is now 1% of the context window with a 1,536-char cap
per entry on `description` + `when_to_use` combined — **checked, all twelve
components are between 239 and 511 chars, so nothing is near it.**

## Rulings

Every ruling taken on the caller's behalf. See the roll-up in the final message.

- **Keep `Task` in every `disallowedTools`, against the research lane's own
  recommendation.** The lane established that `Agent` is canonical and `Task` a
  legacy alias — then stated it had *not* traced the alias through the
  `disallowedTools` permission path specifically. One-level delegation is this
  plugin's only harness-enforced guarantee, so the entry stayed on the untraced
  assumption, which is the right side of "correctness first". It is traced now:
  2.1.251 applies `toolAliases` *before* name resolution, refuses a spawn when a
  host remap would make a name-bound constraint unenforceable, and validates the
  deny list rather than silently dropping a dead entry. The ruling was right and
  the reason is no longer a guess.
  What changes instead is the record: the 2026-08-25 audit called `Task` "a dead
  entry", and it is not dead — it is a live legacy alias. README says so.
- **Type-marking covers the model-facing set; agent bodies stay out of the gate.**
  Cross-cutting 1 asks for agent-body handoff sentences to carry a type word too.
  A gate predicate that must recognise "a line instructing onward routing" needs
  a keyword list, and a keyword list that misses one produces a GREEN gate over
  an unmarked handoff — worse than an honestly narrower scope. So the gate covers
  frontmatter descriptions, the output style, SKILL.md bodies, hook messages and
  README; I marked agent handoff sentences by hand, and the gate's own comment
  states the limit.
- **No type prefix on description openings.** The backlog asks that every agent
  description "open in a form unreadable as a skill". Six descriptions × a
  prefix is always-on static for a signal the Agent and Skill tool listings
  already carry structurally — and the observed misroute was a *cross-reference*
  (`deepwork` read out of prose as an agent), not a listing misread. The style's
  dispatch rule plus type-marked cross-references answer the observed failure;
  the prefix answers a hypothetical.
- **B9 — designer Review mode: cut the mode, keep the rule.** Neither
  keep-and-declare nor a plain cut. The mode contradicts the frontmatter and
  forces an unguaranteeable heuristic into a hook whose whole value is that it
  never cries wolf; but its pinned content (`designer-review-mode-locations`)
  is a *reporting* rule, not a mode — name the location and the measured number,
  never "consider improving accessibility". So the `## Review mode` section goes
  and the rule moves into designer's ordinary reporting. Frontmatter becomes
  true, the hook needs no heuristic, and
  `verify-deliverables.test.mjs`'s designer case stops being a pinned false
  positive and becomes a correct assertion. Costs: a caller wanting a
  critique-only UI audit routes to `review`'s Interface lane instead.

### R3 rulings

**The name stays `omc-slim`, and that is deliberately not my decision to make
differently.** Part I §1.3 is right that the name reads as a diet fork of a
project whose philosophy this one rejects, and the cost of renaming never gets
lower than it is today — there is no adoption signal and no marketplace listing
yet. But a project's name is its owner's identity call, not a reviewer's, and
renaming unasked is the largest possible unwanted scope. So the ruling is: keep
it, put the migration path on record so the decision is cheap when the maintainer
makes it, and fix the trigger. **Marketplace `renames` maps plugin names —
verified against the 2.1.246 binary, where it sits beside `sourceIsFallback` and
`declaredSource` — so a rename is supported.** Decide before the marketplace
listing, because that is the moment the cost stops being bounded.

**Gem 6, the handoff mechanism: adopt neither a seventh skill nor a new format.**
`deepwork`'s progress file already holds objective, decisions, dead ends and next
action, and already requires the next action to be executable without reading
anything else. What was missing was one sentence saying it is the handover — that
you write it and start fresh rather than compacting in place. A seventh skill
would cost static listing budget in a 1%-of-window competition and duplicate a
native memory surface for a value the existing file already delivers.

**Gems 1 and 2 were already present, and gem 2's precondition is why we know.**
The backlog required an axis-overlap check before adding a slop-comment lane.
Run: `checklists.md` already flags "a comment restating the code, or contradicting
it" and "narration comments the change added" in the Simplicity lane, and
`review`'s output already opens `Review: <ship | fix first | needs a decision>`.
Two adoptions closed at zero tokens by checking first.

**Gem 5 moved off the static surface after being written into it.** The style
already says "do not wait on them — you are notified when they finish", which
structurally blocks the unread-result case; the rule earns its place where
re-dispatch actually happens, which is a staged run re-running a lane after
remediation. 53 static tokens saved by challenging my own placement.

## Wagon status

| Wagon | State | Gate |
|---|---|---|
| R0 instrument probe | **half-answered, and that is final for this run** | see below |
| R1 v0.9.2 correctness | **complete, gated, committed** | 2 gates run, 15 findings, all remediated |
| R2 v0.9.3 compression | **complete, committed** | 285 on-invoke tokens out, 70 static; ratchet unpaid and stated |
| R3 v0.9.4 adoption/position | **complete** | gems, rulings, NATIVE.md, README pass, CI, 650-token sibling |
| R4 measurement | not started | |
| R5 v0.9.5 restructuring | not started | |
| R6 v1.0 | not started | |

### R0 — what the probe answered, and what it cannot

`claude plugin eval --help` resolves on 2.1.251 (2026-08-29) with the full option
set: `--ablation with-without` as the default, `--judge-model` defaulting to
haiku, `--max-cost-usd`, `--mocks`, `--threshold`, and an `eval init`. The
manifest hook is `experimental.evals`. Zero official documentation and no
changelog entry, so it is experimental rather than GA.

**Whether execution works or is server-gated is NOT answered, and will not be
this run.** Finding out costs money, and the standing decision is no paid runs.
Help text cannot exclude a server-side gate.

This has a consequence that must not be absorbed silently, so it is written here:
**exit criterion 7's eval clause is unsatisfiable this run**, and so is the
contingency the backlog wrote for it — `smoke-contracts.sh --execute` spends real
money too. R6 records this as a criterion amendment rather than quietly passing.

### R1 — what landed

Every item, with its evidence:

- All 19 re-verified audit findings fixed (A1/M4, B1, B3–B10, C1–C11).
- The `review` cap: measured, not estimated. 5,298 real tokens → 4,985, output
  contract closing at token ~4,860. The gate now checks pin POSITION across every
  skill and refuses to print a corrected figure without a tokeniser.
- Type-marking: 32 references gated. Style carries the dispatch rule.
- Hooks: 24 + 22 cases, 59 mutants, all four suites green.
- codemap script: 10/10, each new case verified failing against the unfixed file.
- Seven previously unpinned behaviours pinned; `COVERAGE.tsv` rows can now name
  any repo-relative `*.md` path.
- Incidents ledger opened in `MAINTAINERS.md`; the aider-watch-mode refusal
  recorded as expired.
- Gates: 297/297 coverage · 116/116 reinforcement · 11/11 shell · 6/6 evals.

## The strategic pass, 2026-08-29

Five research lanes, commissioned with an explicit instruction to look at every
corner rather than the good ones. Findings that changed decisions:

**The moat sentence was false.** `VIABILITY` §0 claimed no head-to-head benchmark
of any orchestrator plugin against stock Claude Code existed anywhere. There are
at least five. One (Laszlo, 2026-06-11) ran 500 tasks with significance testing:
correctness +2.2pp **not significant**, tokens +625k **significant**. One (Ahmed,
2026-04-13) reported this project's two headline findings — cheaper, 2–3× tighter
variance — four months earlier, at n=6 per arm across three complexity tiers,
against this project's n=3 on one task shape. Corrected in part I and in the
assessment.

**Detectors do not work, so the slop defence is craft.** Weber-Wulff (2023):
none of fourteen tools reached 80% accuracy. Liang (2023): seven flagged 61% of
supervised TOEFL essays as machine-written. Every condition that breaks a
detector applies here. Measured, this repository is clean on nine of twelve
dimensions and fails exactly the two a reader sees without reading. Both fixed,
and `scripts/check-prose.sh` keeps them fixed.

**A 42% false-positive rate has a machine-checkable half**, and this plugin had
neither half. The hook now catches an asserted verification with no check in the
transcript, at zero token cost, using a payload field the harness added this
month.

**`observer:` is in the binary.** The advisor pattern and the librarian role,
native, experimental, undocumented, with an `ObserverReport` tool and a framing
prompt that names *prior art* explicitly. Recorded in `NATIVE.md`; nothing built
on it.

**Six problem classes have owners and this project names none of them**, and two
more are genuinely unclaimed — drift detection and onboarding quality. Both are
closer to what this plugin is good at than orchestration is.

**The corpses died of becoming products.** Four of five successor projects are
smaller than what they replaced. Three projects with 5k–24k stars are dead
without archive banners; SuperClaude had four people file an issue in ninety
days. The absence of an archive banner is not a life sign.

## The adversarial pass, and what it cost to be right

The audit lane's finding is the most important thing produced in this run, and it
was not on the backlog:

> Every gate in this repository is one-directional. Each checks that a listed
> thing still exists; nothing checks that an existing thing is listed.

Six exploits, all reproduced, all now caught. The one that should stay
uncomfortable: `disallowedTools` is called *"the only harness-enforced guarantee
this plugin has"* **inside the gate file itself**, and nothing asserted it. That
is not an oversight in a corner — it is the central guarantee, unguarded, in a
repository whose entire pitch is that its checks mean something.

Four guards failed open and silent. The mutation runner **scored a hung harness
as a kill**, so the number this project cites most often as evidence of quality
had a failure mode that inflated it.

**What that says about the apparatus, stated plainly.** The gap between
"301/301, 120/120, 56/56 killed" and what those numbers establish was, until this
pass, larger than any individual defect in the repository. The gates are better
now and the gap is not closed: `check-reinforcement.sh` is still substring
containment inside a paragraph, so it cannot distinguish a rule from a
sufficiently careful rewrite of it. That is the contradiction sweep's job, and
the contradiction sweep is a model reading prose, not a check.

**Two process failures of mine, recorded because they cost real work.** I ran
`git add -A` twice while writer lanes were mid-edit, so two lanes' work landed
under commit messages describing something else. And I ran a full-gate exploit
test while a concurrent lane was changing a file the token ceiling derives from,
so the gate exited early on a stale figure and my test proved nothing until I
re-ran it in isolation. Both are the same mistake: **treating a shared working
tree as if it were mine alone.** The preflight conflict scan exists to catch
exactly this and I did not extend it when I added lanes mid-run.

## Dead ends

- **Getting `review/SKILL.md` under the cap by deletion alone does not work.**
  The compression report predicted this ("needs ~1,900+ chars the duplicate pool
  does not hold") and it was right. What worked was three moves together:
  offloading report-time material to the always-read sibling, moving one rule to
  where it is actually used, and in-place compression of pin-dense prose. Roughly
  1,600 chars came out; a pure-deletion attempt stalled around 400.
- **Joining wrapped lines to save tokens buys nothing.** Tried it on four
  paragraphs; whitespace is close to free in BPE and the readability cost is
  real. Reverted.
- **Moving `## Output` earlier to dodge the cap makes it worse**, because §7
  Close's two pinned rules then cross the line instead. Measured before
  believing it.

### R1's gate: the contradiction sweep

**Eleven contradictions, six introduced by this release. All eleven remediated,
all gates re-run green.** The sweep earned its place as a release gate on the
first run — it caught defects that every presence check passed.

The two that mattered:

1. **Deleting `designer`'s Review mode left a critique-only visual audit with no
   owner.** designer → `review`'s Interface lane → "judgement calls go to the
   designer". A closed loop, and `review` cannot absorb it because it needs a
   diff to exist. Fixed by making designer audit *and* ship the mechanical fixes
   — the same posture `review` itself takes. This was a defect I introduced while
   fixing another one, which is the argument for the gate in one line.
2. **`fixer` gained a rule justified by hook behaviour this same release
   removed.** The rule said the hook would report the agent as having written
   nothing; the hook now says it cannot tell. That is `51dfbcc` exactly: the rule
   survives, its reinforcement goes false, every presence check stays green.

Also fixed: the gate section told the model to dispatch a skill through the Agent
tool one release after adding a rule against that; the re-review budget was
scoped two ways in two files; `tracer`'s entry condition and its refusal named
different sets; the self-identification line was a preamble the same file forbids
and an existing eval case would have failed it; README kept the tier vocabulary
the style had dropped; README claimed a de-duplication in `ROUTING.md` that never
happened; the gate marker had two formats; a stale count of surfacing acts; and
`verification-planning`'s frontmatter said it only plans while its body read as
instructions to perform.

### R1's second gate: the seven-seat review

**One Critical, three Required, ten Optional. All remediated.**

The Critical is the one worth remembering. `scripts/check-coverage.sh`'s
GitHub-description block still divided by 1.135 — the exact constant the whole
release exists to delete — and it survived because it is the one block a reviewer
skips with `OMC_SLIM_SKIP_REMOTE=1`. I ran the gate with that flag every single
time. Obeying it would have published 4,254 tokens to the repository's front page
while the README said 4,405.

**The lesson is not "I missed a line". It is that I skipped a check for a whole
release and never noticed I was skipping it.** The flag is now documented in the
README, and `MAINTAINERS.md` has a release checklist whose first line is: do not
set it at release time.

Three Required, each proved before it was fixed:

- A write through a **symlinked directory inside the project** resolved outside
  the root and drew "nothing in the project changed" — false about a file the
  user can see. Reproduced, fixed lexical-first, pinned by a case and a mutant.
- One path inside a **git submodule** made `git check-ignore` fail the whole
  batch, silently downgrading nested-`.gitignore` handling repo-wide behind a
  diagnostic that named the wrong cause. Reproduced on a real superproject.
- The **self-named rival** warning rendered "omc-slim (omc-slim)" with nothing to
  act on, while holding the install path it needed.

The reviewer also recorded that the tree moved four times underneath it and
gained a new executable component mid-review. That is a fair process criticism
and it is on me: the gate should have run against a frozen diff.

### R1's third lane: the upstream sweep

Six pins advanced, each after its diff was read. One divergence recorded
(`no-polish-for-its-own-sake` — upstream replaced the prose with a structural
bound omc-slim does not have). One attribution re-sourced: `bisect-on-failure`
was tagged `omo-slim` and `git grep bisect` returns nothing in that repository at
either revision. Three claims in VIABILITY part I corrected in place.

And one defect adopted immediately because it was a defect rather than an
addition: **`review` could not see untracked files.** `git diff` contains no
untracked path, so a brand-new file was invisible to a review — and review runs
before the commit, which is exactly when new files exist. Proved in a scratch
repo, fixed in `base.sh`, pinned by three cases.

### R2, and the do-not-touch list challenged

285 real tokens out of on-invoke, 70 out of static, every cut behind Rule 0 and
both gates green after each.

**The ratchet is not paid back. 4,197 → 4,405 → 4,335, net +138 across two
releases.** Safe compression of the style body yielded 17 tokens against a
documented ~3% ceiling. Going further means the roster bullets, whose
justification is now *stronger* than when it was written — the skill listing
budget is 1% of the context window, shared with every other plugin, and one
observed machine had 24 of 103 skills with no description at all. So the number
is stated rather than manufactured, and v0.9.4 is bound to zero growth.

**The do-not-touch list: five of six entries hold, one does not.**

| Entry | Verdict |
|---|---|
| deep-interview approval gate | **holds** — +14.50 points, the largest measured effect in the evidence |
| codemap announce gate | **holds as a gate**, but see below: the real question is whether codemap ships at all |
| "Never simplified away" | **holds absolutely** — safety floor, no challenge |
| roster bullets | **holds, and is better evidenced now** than when written |
| reference siblings "pin-dense, already compressed" | **the reason is false** |
| measured-evidence sentences without Rule 1 | **holds** — the harness needs paid execution, which this run does not do |

On the fifth: measured, `review/checklists.md` runs 238 tokens per pinned rule
and `deepwork/depth.md` 243, against 43 for the output style and 59 for
`review/SKILL.md`. They are the two *least* pin-dense files in the estate, not
the most. The conclusion still stands — the content is enumerative, and a
checklist pins its section rather than each item — but the stated reason was
wrong, and a claim that is right by accident will be wrong next time.

**The challenge that outgrew compression: should `codemap` ship at all?**
`NATIVE.md` already calls it the weakest row in the partial-overlap table. It
creates the stale-markdown artefact class users complain about most; its defect
list is the longest of any component; the keep/cut audit rates it "Cannot, with a
caveat"; and it is the only component that mutates the user's repository. That is
a roster decision, not a compression one, and it belongs to R5.

## Next first action

Start R2 (compression, phases 1-2) from `docs/COMPRESSION-2026-08-28.md` §5.
Rule 0 first: the three pin ADDITIONS come before any cut — fixer's
grep-every-caller, fixer's both-scopes/ToolSearch block, and the "unsearched tool
is invisible" line in both writers. Then the safe band. `review/SKILL.md` has
only **17 tokens** of margin under the re-attach cap and R3's gems need ~30, so
R2 owes it real headroom, not a trim.

One thing the user must run, because it is outward-facing and I will not:
`gh repo edit --description "... six agents, six skills, two advisory hooks — ~4,405 tokens of static context ..."`.
Until then `./scripts/check-coverage.sh` exits 1 on a clean tree, correctly.

## v0.9.7 — the contradiction sweep run as a gate (2026-08-29, later)

Criterion 5 said the sweep had lapsed on v0.9.5 and v0.9.6 and that "a release
gate the author stops running between releases is not a gate". Ran it. Two
independent lanes over all eighteen prompt files, split here because a specialist
cannot fan out. Eleven contradictions, every one reproduced against the file
before it was touched, none of which any presence check could see.

Four were an output contract with no slot for what the file's own rule demands
(`fixer`, `designer`, `review` nits, `verification-planning` step 4). Three were
one word carrying two meanings. Two were wiring claims the harness contradicts
(`tracer` claiming dispatch from an agent that denies `Agent`; `codemap` running
`update` before Step 4 writes the map it stamps). Two were a skill and its
sibling disagreeing with both live in the same pass.

Findings and resolutions are in the CHANGELOG entry for v0.9.7. What belongs here
is what the run taught about the apparatus:

- **The reinforcement gate fired twice on me, correctly.** Compressing
  `review`'s confidence rule to fit the 5,000-token cap cut "because the cost of
  missing it is asymmetric" — the reasoning that makes the rule fire. And the
  clean way to resolve `explorer`'s contradiction was NOT to edit the pinned
  sentence but to keep it whole and scope the exception after it. Both restored.
  This is 51dfbcc's failure mode reached by compression rather than deletion, and
  it took the gate to catch it both times.
- **A pinned pattern can outlive the rule's correct wording.**
  `merge-uncheckable-stages` froze the phrase of the very sentence the sweep
  found wrong. The row was re-pointed at the corrected rule rather than the fix
  being reverted. That is the allowed path and it must stay loud, because it is
  also how a real regression would be waved through.
- **The prose gate caught my own CHANGELOG entry.** Six paragraphs opening on a
  bolded lead-in, four over threshold. The release note announcing an anti-slop
  release read as slop. Rewritten.
- **`review` sits at 4,998 of 5,000.** Two tokens. The next addition to that file
  has to remove something first, and `checklists.md` is where it goes.

Ruling: `--terse-invoke-real` was added to `measure-context.sh` rather than
computing the ceiling inside `check-coverage.sh`, so the table and the gate
cannot count different sets. Costs one more subprocess per gate run.

Ruling: the catch-all precedence rule was taken at +70 always-on tokens. A router
that sends explorer work to `general-purpose` wastes the whole 4,413, so the
trade is 70 against 4,413, not 70 against nothing.

Still blocked on money, unchanged: criteria 1, 2 and half of 3.

Next first action: run `OMC_SLIM_SKIP_REMOTE=1 ./scripts/check-coverage.sh` and
`./scripts/check-prose.sh`; both must exit 0. Then decide whether v0.9.7 is
tagged, which needs the user's call on the GitHub description update that
`check-coverage.sh` prints as `STALE REMOTE`.
